port: "8080"

db:
  username: "postgres"
  host: "localhost"
  port: "5436"
  dbname: "test"
  sslmode: "disable"

minio:
  endpoint: "localhost:9000"
  bucket: "chukki"
  rootuser: "root"
  ssl: false

redis:
  addr: "localhost:6379"